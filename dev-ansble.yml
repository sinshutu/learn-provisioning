- name: Ansble Sample 1
  become: yes
  hosts: vagrant
  tasks:
    - name: add nginx package
      apt:
        name: "{{ item }}"
        state: present
        update_cache: yes
      with_items:
        - nginx
